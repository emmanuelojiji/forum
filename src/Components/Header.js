import "./Header.scss";
import { useState } from "react";

const Header = ({menuOpen, setMenuOpen}) => {
  

  return (
    <header>
     <h2>Work Party</h2>
   
    </header>
  );
};

export default Header;
